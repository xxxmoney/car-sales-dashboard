[project]
name = "car-sales-dashboard"
version = "0.1.0"
description = "Analytical dashboard for used cars"
authors = [
    {name = "Jakub Hána"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
